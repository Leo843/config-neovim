" enable recursive fuzzy find from the current director
set path+=**
set path-=/usr/include
" using ':find something' will find all files matching 'something' in the
" current directory and all sub-directories.
